/*#include<iostream>

using namespace std;

int main()
{
	/*int n = 0, sum = 0;
	std::cin >> n;
	for (int i (0); i < n; i++)
	{
		int x, y;
		std::cout << "Enter";
		std::cin >> x >> y;
		x = reverse::reverseit(x);
		y = reverse::reverseit(y);
		sum = reverse::reverseit((x + y));
		std::cout << sum << endl;
	}
	return 0;
}

class reverse
{
public:	
	static int reverseit(int i)
	{
		int a, b, c, sum = 0;
		while (i != 0)
		{
			a = i % 10;
			sum *= 10;
			sum += a;			
			i /= 10;
		}*/
		//return 0;
//}
//};
