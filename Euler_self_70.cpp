#include<iostream>

using namespace std;

/*int fi(int n)
{
	int result = n;
	for (int i = 2; i*i <= n; i++)
	{
		if (n % i == 0)
			result -= result / i;
		while (!n%i)
		{
			n /= i;
		}
	}
	return result;
}*/

int main70()
{
	return 0;
}