#include<iostream>
#include<algorithm>

using namespace std;

//bool arr[200] = {1,1};
//int arr1[20];

int main100()
{
	/*int t = 0, c = 0;
	{				//Sieve
		for (int i = 2; i < 200; i++)
		{
			if (arr[i] == 0)
			{
				for (int j = 2; i*j < 200; j++)
				{
					arr[i*j] = true;
				}
			}
		}
	}
	int index = 0;
	for (int i = 0; i < 200; i++)
	{
		if (arr[i] == 0)
		{
			arr1[index] = i;
			if (index == 19)
				break;
			index++;			
		}
	}
	int arr2[6840];
	for (int i = 0; i < 18; i++)
	{
		for (int j = i + 1; j < 19; j++)
		{
			for (int k = j + 1; k < 20; k++)
			{
				arr2[c] = arr1[i] * arr1[j] * arr1[k];
				c++;
			}
		}
	}
	sort(arr2, arr2 + 6840);
	int offset = 0;
	for (int i = 0 ; i < 6840; i++)
	{
		if (arr2[i] == 30)
		{
			offset = i;
			break;
		}
	}
	cin >> t;
	while (t--)
	{
		int a, b = 1;
		cin >> a;		
		cout << arr2[offset + a - 1] << endl;
	}*/
	return 0;
}