#include<iostream>

using namespace std;

//bool arr[731];
int c = 0;

/*int yo(int n)
{
	int r = n, a = 0, sum = 0;	
	while (n)
	{
		a = n % 10;
		a = a * a;
		sum += a;
		n /= 10;		
	}
	if (arr[sum] == true)
	{
		return -1;
	}
	arr[sum] = true;
	c++;	
	return sum;
}*/

int main67()
{
	/*int n = 0, x = 0;
	cin >> n;
	for (int i = 0; i < 731; i++)
	{
		arr[i] = 0;
	}
	for (;;)
	{
		n = yo(n);
		if (n == -1 || n == 1)
		{
			break;
		}
	}
	if (n == 1)
		cout << c << endl;
	else
		cout << -1 << endl;
	cin >> n;*/
	return 0;
}