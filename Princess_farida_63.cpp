#include<iostream>

using namespace std;

/*int max  (int a, int b)
{
	if (a >= b)
		return a;
	else
		return b;
}*/

int main63()
{
	/*int t = 0;
	cin >> t;
	for (int x = 1; x <= t; x++)
	{
		int n = 0, temp = 0, exc = 0, exc1 = 0;
		cin >> n;
		int *arr = new int[n];
		arr[0] = 0;
		int  inc = arr[0];
		for (int i = 0; i < n; i++)
		{
			cin >> temp;
			arr[i] = temp;
		}		
		for (int i = 0; i < n; i++)
		{	
			inc = max(exc + arr[i], inc);
			exc = exc1;
			exc1 = inc;
		}
		cout << "Case " << x << ": ";
		cout << inc << endl;
	}*/
	return 0;
}